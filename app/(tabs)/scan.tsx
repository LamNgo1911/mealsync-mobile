import { View } from "react-native";

// This is a placeholder screen for the scan tab.
// The actual scan functionality is handled by the center button in CustomTabBar.
export default function ScanScreen() {
  return <View />;
}
